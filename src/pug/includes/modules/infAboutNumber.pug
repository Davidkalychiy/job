mixin infAboutNumber(bold_text, text, name_img)
    .block_inf
        .inf
            img(src=`assets/img/${name_img}.svg`)
            .descr 
                span(class="bold")= bold_text 
                <br>
                span= text
        .hr