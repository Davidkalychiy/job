mixin toggle(title,value)
    label= title
        .wrapper_toggle
            .wrapper_toggle__btn
                .toggle_around
                    .toggle_circle
                .toggle_text= value